(()=>{const dialog=document.querySelector('.portfolio-dialog');if(!dialog)return;let trigger;const img=dialog.querySelector('img');document.querySelectorAll('[data-preview]').forEach(button=>button.addEventListener('click',()=>{trigger=button;img.src=button.dataset.preview;img.alt='Parte inicial do site '+button.dataset.name;document.getElementById('preview-title').textContent=button.dataset.name;dialog.showModal()}));dialog.querySelector('.preview-close').addEventListener('click',()=>dialog.close());dialog.addEventListener('click',event=>{if(event.target===dialog){const r=dialog.getBoundingClientRect();if(event.clientX<r.left||event.clientX>r.right||event.clientY<r.top||event.clientY>r.bottom)dialog.close()}});dialog.addEventListener('close',()=>trigger?.focus());})();
